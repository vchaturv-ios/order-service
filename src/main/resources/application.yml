server:
  port: 9191

spring:
  zipkin:
    base-uri: http://localhost:9411/zipkin/
  application:
    name: ORDER-SERVICE
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
    username: sa
    password:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
logging:
  file:
    name: /Users/vishnu/Desktop/microservice.log